@model IReadOnlyList<MoneyTransfer.Application.Services.Payments.PaymentListItemDto>

@{
    ViewData["Title"] = "Payments";
    ViewData["BackUrl"] = ViewBag.BackUrl as string ?? Url.Action("Index", "Projects", new { area = "Org" });
    ViewData["BackText"] = ViewBag.BackText as string ?? "Back to Projects";

    var projectId = (Guid)ViewBag.ProjectId;
    var backUrl = ViewBag.BackUrl as string;
    var backText = ViewBag.BackText as string;
}

<div class="page-header">
    <h1>Payments</h1>
    <p>Review project payments and record new incoming amounts.</p>
</div>

<div class="section-card">
    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="alert alert-success">
            @successMessage
        </div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="alert alert-danger">
            @errorMessage
        </div>
    }

    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h4 class="mb-0">Payment Records</h4>
        <a asp-area="Org"
           asp-controller="Payments"
           asp-action="Create"
           asp-route-projectId="@projectId"
           asp-route-backUrl="@backUrl"
           asp-route-backText="@backText"
           class="btn btn-primary">
            Record Payment
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="empty-state">
            No payments found for this project.
        </div>
    }
    else
    {
        <div class="table-container">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Payment Reference</th>
                        <th>Status</th>
                        <th class="text-end">Amount</th>
                        <th>Payment Method</th>
                        <th>Description</th>
                        <th>Date</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var payment in Model)
                    {
                        <tr>
                            <td>@payment.PaymentReference</td>
                            <td>
                                @if (payment.IsVoided)
                                {
                                    <span class="badge bg-secondary">Voided</span>
                                }
                                else
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                            </td>
                            <td class="text-end">$@payment.Amount.ToString("0.00")</td>
                            <td>@payment.PaymentMethod</td>
                            <td>@payment.Description</td>
                            <td>@payment.Date.ToString("dd/MM/yyyy") @payment.CreatedAt.ToString("HH:mm")</td>
                            <td class="text-end">
                                @if (!payment.IsVoided && User.IsInRole("Admin"))
                                {
                                    <form asp-area="Org"
                                          asp-controller="Payments"
                                          asp-action="Void"
                                          method="post"
                                          class="d-inline"
                                          onsubmit="return confirm('Void this payment? This will create a reversing ledger entry.');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@payment.Id" />
                                        <input type="hidden" name="projectId" value="@projectId" />
                                        <input type="hidden" name="backUrl" value="@backUrl" />
                                        <input type="hidden" name="backText" value="@backText" />
                                        <button type="submit" class="btn btn-sm btn-outline-danger">
                                            Void
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <span class="text-muted">No actions</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>